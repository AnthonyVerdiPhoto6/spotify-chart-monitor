name: Cron Test

on:
  schedule:
    - cron: "2-59/5 * * * *"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print time
        run: |
          echo "Cron test ran"
          date -u
